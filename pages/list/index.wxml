<!--pages/list/index.wxml-->
<view>
<view class="list-container" wx:for="{{list}}" wx:key="index">
  <image class="img" src="{{item.url}}"></image>
  <view class="tip">美女</view>
  <view class="name">{{item.name}}</view>
  <view class="price">{{item.price}}</view>
</view>
</view>
